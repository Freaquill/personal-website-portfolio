{
  "name": "fadly-portfolio-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "python -m http.server 4173 --bind 127.0.0.1",
    "check:visual": "node scripts/visual-smoke.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.52.0"
  }
}
